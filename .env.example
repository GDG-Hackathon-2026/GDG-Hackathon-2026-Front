# 로컬 개발용 — 브라우저/SSR에서 백엔드 API 호출 시 사용.
# 실제 값은 .env.local 에 복사해 두거나, Docker build 시 --build-arg 로 주입됨.
NEXT_PUBLIC_API_URL=http://13.125.197.228:8080

# EC2 배포용 — ~/app-web/.env 에 들어가는 값. compose 의 image 태그 치환에 쓰임.
# 로컬에서는 사용하지 않으며, EC2 에는 이미 설정되어 있음 (권한 600).
DOCKERHUB_USERNAME=jiseong02
